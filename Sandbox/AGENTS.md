# Sandbox AGENTS Instructions

These instructions apply to the contents of the `Sandbox` project.

- Maintain 4-space indentation and UTF-8 encoding.
- Keep the sandbox focused on command-line testing of ServUO scripts.
- Expand the interactive shell to support invoking script methods and displaying results.
- When modifying code, run `dotnet build` for validation.
